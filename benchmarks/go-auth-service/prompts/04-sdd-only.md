# Вариант 4: OpenSpec SDD без execution-state

Это новый пустой контекст без истории предыдущих решений. Прочитай находящиеся
в проекте `openspec/config.yaml` и change `auth-service`, затем реализуй все
задачи Apply напрямую базовыми возможностями coding agent. Не загружай skills,
не создавай execution state и не выполняй Archive.
